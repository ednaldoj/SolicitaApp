<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solicita">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application android:name=".MyApp"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.CadastrarDiscenteActivity" ></activity>
        <activity android:name=".activity.SolicitarDocumentosActivity" />
        <activity android:name=".activity.ConfirmacaoRequisicaoActivity" />
        <activity android:name=".activity.ListarDocumentosSolicitadosActivity" />
        <activity android:name=".activity.AdicionarPerfilActivity" />
        <activity android:name=".activity.RedefinirSenhaActivity" />
        <activity android:name=".activity.InformacoesDiscenteActivity" />
        <activity android:name=".activity.HomeAlunoActivity" />
        <activity android:name=".activity.EditarPerfilActivity" />
        <activity android:name=".activity.AlterarSenhaActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>